import React from 'react';
import './Contact.css';

const Contact = ({refProp}) => {
  return (
    <div ref={refProp} className='contact__container'>
      
    </div>
    );
}

export default Contact;